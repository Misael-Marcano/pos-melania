/**
 * Fase 2 del plan: base Jest — sustituir/ampliar por tests de integración reales.
 */
describe('smoke', () => {
  it('el runner de tests está operativo', () => {
    expect(1 + 1).toBe(2);
  });
});
